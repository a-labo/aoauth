/**
 * Test case for twitterStrategy.
 * Runs with mocha.
 */
'use strict'

const twitterStrategy = require('../lib/strategies/twitter_strategy.js')
const assert = require('assert')
const co = require('co')

describe('twitter-strategy', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Twitter strategy', () => co(function * () {

  }))
})

/* global describe, before, after, it */
