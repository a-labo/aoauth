/**
 * Test case for googleStrategy.
 * Runs with mocha.
 */
'use strict'

const googleStrategy = require('../lib/strategies/google_strategy.js')
const assert = require('assert')
const co = require('co')

describe('google-strategy', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Google strategy', () => co(function * () {

  }))
})

/* global describe, before, after, it */
