/**
 * Test case for facebookStrategy.
 * Runs with mocha.
 */
'use strict'

const facebookStrategy = require('../lib/strategies/facebook_strategy.js')
const assert = require('assert')
const co = require('co')

describe('facebook-strategy', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Facebook strategy', () => co(function * () {

  }))
})

/* global describe, before, after, it */
