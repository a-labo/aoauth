/**
 * {{pkg.description}}
 * @module {{pkg.name}}
 * @version {{pkg.version}}
 */

'use strict'

const aoauth = require('./aoauth')

let lib = aoauth.bind(this)

Object.assign(lib, aoauth, {
  aoauth
})

module.exports = lib
